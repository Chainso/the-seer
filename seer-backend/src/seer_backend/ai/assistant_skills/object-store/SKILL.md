---
name: object-store
description: Use when the user wants to search the latest known object snapshots, filter the current object population, or locate candidate objects before drilling into history.
allowed-tools: history.latest_objects history.object_events
---

# Object Store

Use this skill when the user needs to search across the current object set rather than inspect one already-known object.

Guidance:

- Use latest-object search to find matching objects by type and property filters.
- Move into object history once a specific object or cohort has been identified.
- Keep the answer focused on discoverability and selection, not broad causal claims.
