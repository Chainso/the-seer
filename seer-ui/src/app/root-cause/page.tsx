import { ModuleShell } from "@/components/module-shell";

export default function RootCausePage() {
  return (
    <ModuleShell
      eyebrow="Module"
      title="Root-Cause Lab"
      summary="Root-cause workflow shell. Phase 4 adds ranked hypotheses, scores, and source evidence navigation."
      phase="MVP Phase 4"
    />
  );
}
